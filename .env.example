# K3s-ArgoCD Sandbox local secret values
# Copy to .env and replace defaults before running `make secrets`

POSTGRES_PASSWORD=password
N8N_DB_PASSWORD=password
GRAFANA_DB_PASSWORD=password
N8N_ENCRYPTION_KEY=some-secret-key
